version: 0.0.1
name: navigation
publish_to: none
description: Navigation module

environment:
  sdk: '>=3.0.0 <3.13.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  dashboard:
    path: ../features/dashboard

  rxdart: ^0.27.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
